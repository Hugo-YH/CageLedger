# 故障排查

## 无法访问系统

```bash
docker compose ps
docker logs cageledger
```

检查端口是否正确开放，反代是否指向 `5173`。

## 登录失败

检查默认密码是否已变更、环境变量是否生效、数据库是否被重置。

## 笼位图异常

检查饲养间、笼架、笼位结构和当前账号授权。

## 发布后 Wiki 未同步

检查：

- 是否改动了 `wiki/**`
- 是否推送到 `main`
- `sync-wiki` 工作流是否执行
- Actions token 是否具备 `wiki: write`
