# 项目结构

本页描述当前仓库的实际结构和职责边界。

## 顶层结构

```text
.
├── index.html
├── server.py
├── server_app/
├── src/
├── assets/
├── scripts/
├── wiki/
├── docs/
├── data/
├── .gitea/workflows/
├── Dockerfile
├── docker-compose.yml
└── docker-compose.offline.yml
```

## 关键目录职责

| 路径 | 职责 |
| --- | --- |
| `server.py` | HTTP 入口、路由分发、兼容主入口 |
| `server_app/config.py` | 环境变量、路径、版本和更新检查配置 |
| `server_app/db.py` | SQLite 连接与初始化辅助 |
| `server_app/cache.py` | 服务端短时缓存与相关辅助 |
| `server_app/repositories/` | 表级 SQLite 读写、分页、payload dump/load |
| `server_app/services/` | 接收、待进驻、数量统计表、结算流程等领域服务 |
| `src/app.js` | 前端启动入口、页面装配、事件委托 |
| `src/api/` | URL builder、fetch wrapper、领域 API |
| `src/domain/` | 日期、IACUC 等纯业务 helper |
| `src/state/` | 本地状态和存储边界 |
| `src/views/` | 页面视图模块边界 |
| `src/print/` | 打印和导出相关模块 |
| `src/styles.css` | 全局样式 |
| `scripts/` | 版本同步、发布、离线打包、API 冒烟等脚本 |
| `wiki/` | 正式公开文档源 |
| `docs/` | 分析、契约、迁移、进度文档 |
| `data/` | 运行数据目录 |

## 目录原则

- `data/` 只存运行数据，不作为常规代码修改对象
- `dist/` 是打包产物目录，不手工编辑
- `wiki/` 变更会同步到 Gitea Wiki
- `docs/` 用于工程化推进，不替代 `wiki/`

## 常用入口

| 用途 | 命令或路径 |
| --- | --- |
| 共享模式 | `npm run dev` |
| 静态模式 | `npm run static` |
| 语法检查 | `npm run check` |
| API 冒烟 | `npm run smoke:api` |
| 离线打包 | `npm run package:offline` |
| 本地发布 | `npm run release:local -- --version X.Y.Z --push` |

## 相关页面

- [[API与数据模型]]
- [[开发规范]]
- [[发布与CI-CD]]
