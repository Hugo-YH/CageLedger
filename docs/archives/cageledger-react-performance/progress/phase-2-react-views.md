# Phase 2: 业务视图迁移

**Status**: Complete

- [x] Task 2.1：应用壳、登录和主页。React Query 管理会话与 bootstrap，登录、主页、侧栏、设置抽屉、登出已迁移。
- [x] Task 2.2：笼位、笼卡、打印和扫码。按房间查询、虚拟化笼位图、单笼/批量编辑、待进驻分配、笼卡录入与分页、打印和摄像头扫码已迁移。
- [x] Task 2.3：饲养费、数量统计表、结算和导出。行级录入、动物数量规则、全库筛选、预览/打印、负责人合表、结算导出和流程发起已迁移。
- [x] Task 2.4：流程中心、房间、账号、数据、日志和系统设置。报销登记与流程推进、基础设施维护、账号权限、IACUC/历史台账导入、审计日志和 Release 更新检查已迁移。

## Notes

- 全部业务视图已进入 React 应用壳，旧兼容导航在 Task 4.1 删除。
- 登出仅清理领域 Query，保留 session observer，E2E 已覆盖该边界。
- 笼卡 API 保持原有保存、删除、确认接收协议。
- 笼卡预约文本解析已提取到独立 domain 模块，Vitest 与桌面/760px E2E 覆盖核心字段。
- 笼位图按房间 Query 缓存，笼架网格按行虚拟化；占用记录和待进驻写入仅失效当前房间。
- 业务页按需加载，生产构建将笼位、笼卡、扫码和 jsQR 分离为独立 chunk。
- 数量统计表行组件自持输入状态，通过 imperative snapshot 保存，单元格输入不刷新整个工作区。
- E2E 默认启动临时数据库并按需创建最小基础设施，覆盖统计表保存、列表刷新和删除，运行数据目录保持不变。
- 流程中心和六个设置页按需加载，服务端数据由独立 typed API 模块管理；房间创建和账号创建/删除使用临时数据库完成写操作回归。
- React 弹窗统一使用 fixed backdrop、可滚动正文和移动端 bottom-sheet 布局，浏览器实测 1280px 与 760px 无页面级横向溢出。
