# Phase P4：大动物异常评估资料接收

- [x] T12 导入目录草案和参考图开发副本。验收：51 张图片齐全、草案链接无缺失、图片无 EXIF/GPS、单文件小于 5 MB；不修改 active 目录和运行数据。

## Notes

- 完成：2026-09-04。
- 来源包含 Markdown、HTML 和 51 张猴、兔、豚鼠、犬参考图，原始体积约 131 MB。
- 开发副本保留目录与文件名，图片最长边压缩到 2048 px，JPEG 质量为 85，导入后约 19 MB。
- 抽查发现原图包含手机型号和精确 GPS 坐标，开发副本已清除全部 EXIF；桌面原件未修改。
- 资源位于 `docs/analysis/large-animal-abnormal-assessment/`，尚未注册新 `moduleCode`，也未写入 SQLite 或当前 active 巡检目录。

## S.U.P.E.R Review

- S：本阶段只接收和整理开发资料，不混入模块实现。
- U：开发资源不被运行时代码引用，不改变现有数据流。
- P：`manifest.json` 明确记录文件、哈希、尺寸与处理规则。
- E：仓库文件不记录桌面绝对路径、设备信息或 GPS。
- R：整套资料可独立替换，不影响现有小鼠异常评估模块。
