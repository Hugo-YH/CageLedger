# Phase P1: 前端在线编辑

- [x] T5 巡检标准页编辑模式（目录树 + 节点表单 + 保存草稿）— 验收：管理员可增删改条目，房间管理员只读
- [x] T6 图片管理（上传/替换/删除/缩略图）— 验收：上传即落盘并绑定节点引用
- [x] T7 发布确认（差异摘要 + 草稿提示）— 验收：发布前显示差异，发布后标准页更新

## Notes

- 完成：2026-08-06。浏览器验收：编辑模式树 248 节点、新增条目自动编号、表单保存草稿 200、发布弹窗差异 1 条、还原后草稿提示消失；房间管理员无编辑入口；图片上传 201 且缩略图路由 200。
- 修复过程：antd v6 弃用（Drawer width→size、Alert message→title、List 移除）；表单转换丢失 inputType（nodeType 不在表单值中）；diff 序列化将 `{url, desc: undefined}` 与 `{url}` 判为不同；parentId 数字/字符串归一。
