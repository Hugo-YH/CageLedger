# GPT-6 Astra 项目指令整理记录

此文用于追溯 2026-09-12 的整理，不是活动任务或每次会话必读入口。当前规则见 [代理指令维护契约](../../contracts/agent-instructions.md)。

## 首轮整理（此前已完成）

2026-09-12 阅读 [OpenAI：Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)。本次将建议用于仓库指令与技能：精确描述用途，按需提供参考，移除过期流程与相互冲突的限制，明确完成范围。模型与推理配置仍由调用端选择。

此前 2026-09-07 已按同作者文章及 OpenAI 指引精简过主入口；此次保留有效结构，并处理遗漏：

| 指令位置                                            | 发现与处理                                                                                                        |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `AGENTS.md`                                         | 明确补充要求与原目标的关系，以及既有授权无需重复确认；保留数据、业务和发布边界                                    |
| `.codex-run/skills/cageledger-standardization-dev/` | v0.5.3 历史任务仍要求加载已迁移的进度表、执行评分与遥测，并引用原生 JS 入口；正文移入历史归档，旧位置仅留兼容链接 |
| `antd-ui-audit`、`improve-animations`               | 区分局部修复与用户要求的全系统审查，避免把全系统任务缩成单页检查                                                  |
| `find-animation-opportunities`                      | 描述与正文统一：分析请求交付建议，实现请求继续完成授权范围                                                        |
| 动效计划、审查与性能参考                            | 移除与入口冲突的自动停工、固定参数、强制新建计划和“减少动画不能为零”等要求；保留实际反馈、键盘、触屏与连续性检查  |
| `testing-strategy.md`                               | 明确隔离测试可自主执行，但复用已有服务不能视为自动隔离；验证按改动选择                                            |

历史技能见 [归档](../cageledger-standardization/skill/SKILL.md)，不是当前任务入口。没有修改个人 Skills、插件缓存、应用中的 AI 模型配置或接口提示词；技能调用策略保持原样。

本次校验文本、引用与技能结构，不宣称通过文档修改证明模型性能提升。官方建议需要结合真实任务表现迭代，不能用字数减少或静态校验代替行为效果。

## 本轮复核补充

用户再次要求对照同一官方文章整理，先核对当前版本，再处理剩余问题，没有重做已有入口，也没有批量关闭技能。

- `AGENTS.md` 的按任务读取、持续完成、业务和数据边界已适用，保留当前结构；`.agents/skills/` 的描述和根路由经复核保持原样。
- 清理 `apple-design`、`emil-design-eng` 和 `review-animations` 直接参考中仍将特定动画 API、固定缩放、错峰和次日复查泛化的措辞。按实际交互和现有实现决定是否采用，保留必要的连续性、无障碍和验证要求。
- `testing-strategy.md` 将 UI 验证从固定前置步骤改为验收要求；完整检查已覆盖的子项不重复执行。四档视口、computed style、业务回归及提交／发布门禁保留。
- `documentation-system.md` 统一引用测试矩阵，消除所有文档都构建文档站与仅 Wiki 变更需要构建之间的歧义。
- 首轮整理经过移入本归档，`agent-instructions.md` 仅保留现行规则、官方依据与检查场景，避免维护指令时重复加载历史。

本轮仅调整项目内 Markdown；不改应用功能、模型设置、个人技能或插件缓存，不提交、推送或发布。10 份修改文档的格式和 Markdownlint、3 个相关技能的结构、7 个相对链接及 `git diff --check` 均通过。没有重跑与文字修改无关的业务回归，也未执行模型行为对照实验。
