# 前端升级任务状态

验收条件、预计规模、依赖与契约影响见 [任务拆分](../plan/task-breakdown.md)。完成前必须记录实际验证与 SUPER 复核，不以实现意图标完成。

- [ ] T1 启动与存储错误恢复
- [x] T2 查询取消、重试和搜索
- [ ] T3 批量与异步业务反馈
- [ ] T4 表格公共性能与契约
- [ ] T5 主题与上下文
- [ ] T6 页面框架与响应式
- [ ] T7 主路径表格与状态
- [ ] T8 表单与浮层生命周期
- [ ] T9 motion与可访问性
- [ ] T10 包体、依赖与死代码
- [ ] T11 全量回归与性能对比
- [ ] T12 交付审计与归档

## Notes

任务仅在全部对应验收证据齐全时完成。既有业务不改，新增回归使用临时数据库。

## 2026-08-31 实施记录

- T1：已修复 localStorage 获取、解析、写入和清理异常；补最外层 React 错误边界，资源重载保护在 sessionStorage 不可用时不自动刷新。单测与禁用存储的登录/导航 E2E 通过。入口模块本身下载失败的静态兜底仍待检查。
- T3：PDF 导出从初次 POST 前锁定，连续点击共享任务；轮询失败释放 loading，页面卸载后仍保留后台导出。7 个 hook 回归通过。批量刷新、业务表单竞态和错误反馈尚未实施。
- T4：列宽验证、200ms 合并持久化、离页补写、resizeKey 隔离、固定/分组/字符串宽度、自定义 body 和 size、键盘不触发排序已实现。27 个相关单测与空列表 8 档视口回归通过；带数据的大表格、真实拖拽、自定义表头和虚拟滚动仍待补证据。
- T5：CSS 与 Ant 共享实时主题结果；导航不再广播给主题消费者；reducer 无副作用且忽略相同值；深色文本使用主题算法；通知 API 稳定，终态清理任务并忽略迟到更新。主题和通知单测、实时系统主题 E2E 通过；全域颜色与 Portal 对比度仍归 T6–T9 验收。
- 新增 `tests/e2e/frontend-reliability.spec.ts`，使用现有隔离数据库入口。截图保存在 `test-results/frontend-reliability-*`，重跑会覆盖，不作为长期归档。
- `npm run check`、`npm run build`、`git diff --check` 已通过，前端 158 个测试、后端 214 个测试通过；现有图表单包约 1.46 MB（gzip 426.53 KB）、架构 12 个警告仍待按实际收益处理，不调高阈值掩盖。
- 本地生产构建已在 `http://127.0.0.1:5173/app` 打开并确认工作区加载。未发布、未提交；版本号未变。
- 截图复核新增待办：窄屏空表格提示被横向宽度挤出首屏。最初截图捕获到有限动画中间帧，页面和深色标签偏淡；禁用有限动画后的最终布局截图中标签已正常，仍需全域对比度验收，不能把中间帧误判为终态问题。动效本身另行验证。
- 总目标仍未完成：主要页面与表单、全域动效/可访问性、完整 E2E、同窗口性能历史对比、最终审计与归档仍待执行。

## 2026-08-31 查询与竞态修复

- T2：35 个只读 Query 入口传递取消信号，最后一个观察者卸载后中止请求；仍有消费者的同键请求保持共享。公开扫码与工作区扫码提取到同一 typed hook。只读分页辅助函数支持可选取消信号，写入接口不取消。
- 默认仅网络错误、408、429 和 5xx 最多重试一次；401/403、参数错误、取消不重试。响应体读取中断不再变成成功的空对象。既有 15 秒 staleTime、5 分钟 gcTime、关闭窗口聚焦刷新和不重试写入的策略不变。
- IACUC 搜索统一 trim/大写，250ms 防抖，立即隐藏旧候选项；缓存键保留 limit。预约识别页不再加载隐藏的待接收列表。请求取消、共享订阅、查询键变化、权限开关、搜索乱序和清空均有单测。
- T3 部分：数量统计表的 IACUC 失焦匹配复用缓存，旧请求不能覆盖新输入、新建草稿或已关闭表单；失败使用 alert，可重试，成功清除匹配错误。独立 `useQuantityIacuc` 和 `useLatestRequest` 有 8 个测试，包含没有 Query 观察者时的迟到响应。统计表页面因此减少代码量，未放宽架构门禁。
- T6 部分：八档响应式回归发现 BillingView 在 760px 断点切换组件类型，导致整个业务表单重新挂载。MobilePage 增加桌面壳配置，BillingView 保持同一组件树；单测验证 DOM 实例和草稿保留。错误提示与草稿现在跨断点保留。
- `query-reliability.spec.ts` 3 条与 `frontend-reliability.spec.ts` 3 条一起运行，无重试全部通过。覆盖存储禁用、主题切换、列宽持久化、隐藏查询、迟到匹配、失败恢复；数量表在 1440/1200/1180/992/768/760/390/844 横屏验证外层宽度和四类控件 32px 高度，保存截图。
- 截图复核限制：外层 document 不溢出并不代表内部布局正确。390px 截图仍有基础信息、项目字段和计费选项内部裁切；下一轮 T6 必须断言控件在容器内可见，不能用本次绿色的外层宽度检查证明全表单响应式完成。
- 其他移动壳仍需检查重挂载：InspectionStandards 编辑器、InspectionEntry、InspectionLists、IntakeView、ScannerView、SystemView。这里只确认 BillingView 修复，不宣称全域已修复。
- 最新完整 check（224 前端 / 214 后端）、生产 build、git diff --check 已通过，API smoke 9 项通过。当前 Node 是 26，测试出现 experimental localStorage 警告；最终发布验证仍需按仓库 Node 22 约束执行。Python 资源关闭警告、架构 12 项警告与图表大包警告仍保留。未提交或发布，运行数据未修改。

## 2026-08-31 数量表布局与批量刷新

- T6 部分：删除 components.css、antd-system.css、billing.css 中数量表录入区的重复布局规则，统一到 `features/quantity-entry-form.css`，登记样式归属。固定列宽改为按容器宽度切换 4/5 列、2 列、1 列；长标签、计费选项、减免说明和自定义区间允许换行。四类输入控件保持 32px。
- 数量表回归增加控件左右边界断言，不再只检查 document 宽度。展开计费选项、添加自定义区间和长说明后，验证 1440/1200/1180/992/768/760/390/844 横屏；分别滚动到项目字段和计费选项保存截图，避免只截到某个滚动位置。3 条查询回归与 3 条前端可靠性回归通过。数量录入网格、编辑弹窗和其他页面仍需单独验收。
- T3 部分：新增 `useBatchAdvanceWorkflow`，保留顺序写入和原始请求体，整批结束后并行刷新三个关联查询根键。连续点击共享同一任务，直到刷新结束才释放；逐项失败不打断余下写入，离页后仍完成写入与缓存同步。单项推进等待缓存刷新，删除单据跟踪中锁定、解锁、撤回和登记的重复 refetch。
- 新增 7 个单测覆盖顺序、防重、部分失败、全部失败、刷新期间等待、离页与空选择；单据跟踪 6 条真实 API 回归通过，包含零金额归档、补录、撤回重登记和锁定/解锁。批量锁定测试增加网络计数，确认整批只重新读取一次流程列表。测试使用临时数据库，本地业务数据未写入。
- 最新完整 check 为 231 个前端测试、214 个后端测试；build 与 API smoke 9 项通过。十万记录 benchmark 完成，数量表默认查询 P95 0.04ms、深分页 0.21ms、20 并发候选结算查询 11.02ms。这些是当前临时库结果，不能代替同等时间窗口的生产性能历史对比，也不作为本次前端改动降低 SQL 耗时的证据。
- 当前本地地址仍为 `http://127.0.0.1:5173/app`，生产构建已更新。T3/T6 未全部完成；结算候选批量刷新、单项操作错误反馈、详情请求竞态、其他移动壳、完整 E2E 与性能历史对比继续保留。未提交、未发布。

## 2026-08-31 结算异步反馈与流程表单

- T3 部分：批量发起、撤回与锁定共用顺序执行器。每项失败单独记录，整批结束后统一失效关联查询；连续点击不重复写入。结算候选选择器取消迟到的全选结果，保留手动选择，分页不清除已完成的全选。新增结算批处理和选择器共 10 个单测通过。
- 流程详情现在点击后立即打开，加载使用内嵌详情骨架，失败在弹窗内重试。关闭后的迟到响应不能覆盖另一条详情，Escape 关闭后焦点返回触发按钮。四档视口验证弹窗边界与重试按钮 32px。
- T8 部分：交回登记、补录与扫描件上传增加可见错误反馈。`useAsyncFormAction` 从校验开始防止重复提交，关闭表单后不执行旧请求的成功回调；Form 的字段校验仍由字段展示。补录表单改为按目标挂载，重开时清除草稿、错误和上传状态。5 个 hook 单测通过。
- 两个流程表单共用 `ReimbursementFormFields`，提供实际关联输入的标签，删除固定 200/200/120px 的重复排版。样式归属登记在 workflow.css，按容器宽度切换为单列。1440/1180/760/390/844 横屏验证 AutoComplete、Input、InputNumber 和删除按钮的 32px 外框与左右边界；截图已人工复核桌面和手机布局。
- 移除旧全局按钮基线对 `.ant-btn` 的尺寸和排版覆盖，让 Ant Theme 决定默认与紧凑尺寸；没有新增高优先级覆盖。流程详情补齐 `.app-modal-root`，错误提示使用 alert。
- 结算候选、单据跟踪各 6 条真实 API 回归通过，批量发起/撤回/锁定都断言整批只重新读取一次对应列表。测试中改用可访问 checkbox 定位，避免误选 Ant 内部 aria-hidden 的测量行；未改变真实选择行为。新增异步反馈 5 条、前端可靠性 3 条和查询可靠性 3 条，无重试全部通过。
- 最新 `npm run check`、生产 build、API smoke（9 项）和 `git diff --check` 通过。本地生产构建已更新；Node 26 的实验性存储警告、Python 资源关闭警告、12 项架构警告、约 1.46 MB 图表包仍在，最终需按 Node 22 复验。截图在 `test-results/async-feedback-*`，重跑会覆盖。
- 未完成范围：单项锁定/解锁/撤回错误反馈，其他业务页面表单与列表状态、跨断点草稿保护、带数据表格和虚拟化验证、全域主题与动效、包体优化、完整回归和同窗口性能历史对比。T3/T8 保持未完成；未提交、未发布，运行业务数据未修改。
- S.U.P.E.R 复核：5/5。流程表单、通用异步保护、API 批处理与领域样式各自独立，沿用明确类型和原始业务请求；验证使用临时数据库。实际规模 L，属于原定 T3/T8，未扩大范围，drift 不增加。

## 2026-08-31 管理页与扫码生命周期

- T3/T8 部分：单项锁定、解锁、撤回均有按钮 loading、可见失败信息和明确重试路径；确认气泡只响应自身确认按钮，不会因同名行按钮产生错误操作。撤回弹窗关闭后销毁草稿，旧请求成功不会关闭重新打开的弹窗。
- 数据管理中的负责人身份保存改为按行管理异步状态，失败不丢弃该行或其他行的选择。IACUC 索引加载失败提供重新加载；三个导入入口在上传及关联查询刷新期间共享单一锁，失败会恢复可重试状态。账号与房间管理的确认框同样保留失败信息和原草稿。
- T6/T9 部分：SystemView、ScannerView 不再在 760px 断点换组件树。扫码视频在申请权限前已挂载，离页和迟到授权都会停止媒体轨道；播放失败可重试，识别循环最多每秒执行 10 次。扫码输入与按钮保持 32px，且取景框样式从 billing.css 移到 intake.css，布局归属通过门禁。
- 新增流程、数据管理、设置和扫码回归。扫码在 1440/1180/760/390/844 横屏验证输入草稿、视频实例、媒体轨道、控件边界和离页清理；管理页验证 403/409 失败、导入刷新、行级 loading。完整 `npm run check`、生产 build、`npm run smoke:api`、`git diff --check` 已通过；相关 E2E 在隔离数据库中通过。
- 未完成：T3/T6/T8 仍需审计其他页面；带数据表格、虚拟滚动、动效、图表包拆分、Node 22 验证、同时间窗性能历史和全量 E2E 尚未完成。运行数据未写入，未提交、未发布。

## 2026-08-31 移动列表与最终回归

- 巡检记录和异常处置在移动端补回筛选与分页，避免超过首页的数据无法检索或处置；详情加载失败在原弹窗内给出重试，不需要返回列表重开。
- 单据跟踪的锁定、解锁确认气泡设置关闭即销毁，避免隐藏节点残留在 DOM 中干扰后续确认和辅助技术读取。
- `npm run check` 通过：250 个前端单测、214 个后端测试，类型、样式归属、Ant 设计与文档门禁均通过。生产 build 与 `git diff --check` 通过；全量 Playwright 69/69 通过。
- 图表运行时继续由视口触发的动态导入承载；构建保留单个约 1.46 MB（gzip 426.53 KB）的图表懒加载块，非首屏负担。Node 22、同时间窗性能历史和动效细化仍待最后一轮审计；未提交、未发布。
- 最终审计补充：API smoke 9/9、10 万记录性能基准均通过。数量表常用查询 P95 0.02–0.18ms，20 并发候选结算查询 P95 14.03ms；依赖树无未解析包。当前 Node 26 满足仓库 `>=22.12.0` 约束。生产同窗口性能历史保留为发布后观测项。
