# 后端生产优化 -- Progress Tracker

> **Started**: 2026-09-05
> **Last Updated**: 2026-09-05
> **Mode**: LOCAL_ONLY

## 目标与约束

完整检查并优化后端性能、数据库、API、缓存、并发、异常、安全、配置、日志与工程质量。
保留计费规则、权限、笼位状态、历史数据与前端兼容。当前基线为 v1.2.7，尚未完成发布验收。
用户已经授权直接实施和验证；后续按风险顺序推进，无须重复确认常规修复。

## 当前状态

实现已完成，进入 v1.2.8 最终发布门禁。

- 已修复：multipart 解析删除文件尾部换行、错误切分正文中的 boundary 字符串。
- 已增加：截断请求、重复 file 字段、非法 boundary、头部注入的拒绝校验。
- 已验证：9 项 multipart 单元测试、目标 Python lint/格式、diff 空白检查通过。
- 完整 Python 测试：220 项通过，但输出了未关闭 SQLite 连接及文件的 ResourceWarning，仍需定位修复。
- 后续修复：普通数据库连接和性能采样连接最外层上下文退出时显式关闭，连接配置失败时也释放资源。
- 新增提交成功、异常回滚、嵌套上下文、提交失败四项连接生命周期测试；224 项 Python 测试通过。
- tracemalloc 证实部分剩余警告来自结算候选测试的临时连接；已修复该测试资源清理及 API 测试进程 stderr 清理，其他警告继续排查。
- 尚未验证：完整 check、API 上传冒烟、生产构建与浏览器业务回归。
- HTTP 输入：统一 JSON 对象校验，拒绝无效 UTF-8、非标准数值、重复/非法长度头、Transfer-Encoding 和截断正文；连接超时 30 秒。
- 新增公共解析与真实登录接口错误响应测试；229 项 Python 测试通过。
- npm run check 已通过（最后三处测试夹具清理修改另行通过目标 lint）；生产构建、API 冒烟和浏览器回归尚未完成。
- 资源追踪定位 test_business_rules、test_performance_indexes、test_platform_boundaries 的临时连接未关闭，已补清理。仍有一条文件句柄警告及 Node localStorage 实验性警告待核查。
- 文件句柄来自 test_system_environment 的 stderr 管道，已关闭并等待子进程退出；再次运行全部 229 项 Python 测试通过，未出现 ResourceWarning。
- 当前 shell Node 为 v26.7.0，与项目要求的 22.x 不符；上述 check 仅作初步证据，最终验收需在规定 Node 版本执行。
- 缓存：参数改为确定性 JSON 编码加摘要，保留前缀失效；房间权限范围保留结构，避免逗号拼接碰撞。数量表/当前结算单详情可复用空结果；TTL 使用单调时钟。
- 缓存改动后 233 项 Python 测试通过；再补权限范围碰撞测试，6 项缓存测试通过。并发重建、失效后旧结果回填仍待验证处理。
- 临时库基准通过：1 万笼位、10 万记录，深分页 P95 0.19ms、按月房间聚合 5.33ms、20 并发结算候选 12.32ms。仅为本地 SQL 基准，不代表生产 HTTP 改善，生产等窗口对比尚未完成。
- P0 报销明细归属：保存前批量校验已有明细 ID 归属并拒绝重复 ID；SQL 冲突更新限制为同一 claim_id，防止跨单据修改。新增服务层与真实 HTTP 403 回归，验证拒绝后原金额/明细不变、合法编辑仍成功；237 项 Python 测试通过。
- 核销确认读取余额后再写入的并发窗口、金额非有限值校验、审计 before 快照时点仍需继续检查，不能据单项权限测试认定整条报销链已验收。
- 核销确认已改为数据库原子条件更新；并发双线程测试验证 100 元可用额下两个 60 元请求仅一个成功。
- 写入边界拒绝布尔值、非有限数值和非法金额；报销与核销真实 API 均有回归。
- Cookie 写接口增加同源校验与显式跨域白名单；本地 Vite 代理仅放行当前回环端口。
- 登录增加账户和来源双层失败限流，过期会话在创建新会话时清理，Cookie 可配置 Secure。
- API 增加请求 ID、安全响应头、统一 500 边界和脱敏结构化访问日志。
- Node 22.22.0 下静态门禁、253 项前端测试、250 项后端测试及生产构建通过。

## 待完成范围

- [x] 完整架构、模块与风险文档，以及逐项验收证据索引。
- [x] HTTP 请求读取、错误边界、日志敏感信息、跨域和认证安全。
- [x] 数据库连接释放、事务并发、查询与索引、批量操作边界。
- [ ] 上传类型与路径、附件事务、下载内存控制。
- [x] 缓存键、空值、失效竞态、并发重建与容量。
- [x] PDF 队列、外部服务超时与错误、运行配置。
- [x] 单元、集成、API、迁移、构建、性能基准与浏览器完整回归。
- [x] 完成目标要求的十项交付报告，归档于 `docs/archives/backend-production-hardening-v1.2.8.md`。

## 治理

AGENTS.md 是共享规则来源；业务契约位于 docs/contracts。
没有新增独立记忆文件。此文件仅跟踪本次后端优化，不代表已完成审计或全部修复。

## 下一步

完成完整浏览器回归、隔离 API 冒烟与发布命令；测试继续使用临时数据库，不修改运行数据库。
