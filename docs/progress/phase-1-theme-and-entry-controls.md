# 阶段 1：主题、样式边界与高频录入

**目标**：消除遗留 Ant 选择器边界警告，统一数量录入的通用控件与确认浮层，验证文档站和四档响应式表现。

## 任务

- [x] **任务 1.1：审计 Ant Design 基线**
  - 优先级：P0
  - 验收：`antd doctor`、`usage`、`lint` 为零问题；官方蓝 `#1677ff` 与主题 Token 契约通过。
- [x] **任务 1.2：完成样式归属边界**
  - 优先级：P0
  - 验收：所有登记样式和组件族状态为 complete；业务 Ant 选择器有作用域；`npm run check:style-ownership` 无警告。
- [x] **任务 1.3：迁移数量网格通用输入与保存确认**
  - 优先级：P0
  - 验收：日期、数字、伦理输入使用 Ant `Input`，类型使用 `Select`，确认保存使用标准 `Modal` 和 `Button`；Tab 顺序保持。
- [x] **任务 1.4：同步文档站与浏览器回归**
  - 优先级：P1
  - 验收：VitePress 表格、按钮和焦点样式复用品牌 Token；桌面、1180px、760px、横屏与 Axe 通过。

## 说明

- 工作流锁定的未提交业务改动保留在本工作区，未与本阶段 UI 改动混淆。
- `legacy.py` 的架构上限已达 7,350 行，本阶段未增加其行数。
