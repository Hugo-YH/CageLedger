# 依赖与执行顺序

```mermaid
flowchart LR
  A[代码与运行基线] --> B1[B1 启动/查询/异步/表格]
  B1 --> B2[B2 主题/布局/列表/表单]
  B2 --> B3[B3 动效/可访问性/包体]
  B3 --> B4[B4 全量回归/审计/归档]
```

只读审查可并行；修改公共状态、Query 和通用组件按依赖顺序完成，避免交叉变更。任务未通过验收时保留未完成状态，不用绿色静态检查代替浏览器证据。
