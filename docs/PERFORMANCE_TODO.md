# Performance TODO

## Pending

- Continue shrinking billing data loads: quantity-sheet, workflow, reimbursement ledger, billing statement, and workflow version lists return summary fields only; billing statements now read current version rows directly instead of scanning all workflow payloads, and single-statement deep links now return summary fields instead of the full statement payload. Billing now queries occupancies by month and scope, the infrastructure occupancy endpoint no longer assembles the full cached state, related slot/rack/room payloads are fetched by referenced IDs, statement generation reuses those referenced infrastructure reads, quantity-sheet statement generation reads only rooms referenced by the current sheets, and the next step is field-level trimming for large statement detail payloads.
- Continue trimming detail payloads: reimbursement detail history now returns ledger summaries instead of full record payloads with IACUC details, workflow event lists now return display summaries instead of full event payloads, workflow detail / version list / line summary queries now use short caches to reduce repeated弹窗查询成本, and reimbursement source context now reads only rooms referenced by the current statement.
- Continue replacing full-list refreshes after writes with targeted reloads or optimistic local updates in the frontend, especially around workflow status changes, quantity-sheet-related derived previews, and any remaining admin tables. Intake batch and placement task writes now merge server responses locally instead of reloading both paged lists after each write, intake batch and placement task page reads now use short server-side caches with write-side invalidation, paged list loads ignore stale responses when users rapidly switch filters or pages, reimbursement/workflow/quantity-sheet saves now update the current page in place instead of immediately reloading the whole list, billing workflow create/advance/delete responses now carry reimbursement summaries so the ledger page can merge workflow-side changes directly, and cage-map / quantity-sheet preview statements now reuse front-end derived-result caches instead of rebuilding month timelines and export forms on every render.
- Continue reducing permission-filter overhead for room managers: entity list filtering now uses structured room/rack/slot fields instead of assembling the full state for each restricted list response, and placement-task pagination now pushes authorized room filters into SQL.
- Continue reducing repeated audit-log queries: operation-log pagination now uses a short cache and audit writes invalidate the cached pages.
- Continue shrinking repeated data scans inside billing derivations: PI summary fields now read from `stateIndexes()` pre-aggregation instead of rescanning `IACUC_INDEX` and occupancies on each preview, IACUC-to-PI summary and PI option rows now reuse front-end caches between index refreshes, PI option rows from billable occupancies and quantity sheets are now emitted by `stateIndexes()` instead of remapped on each option render, quantity-sheet save validation now reads only the target room payload instead of scanning all rooms, and date-based unit-price / discount lookups now reuse short front-end caches.
- Continue shrinking persisted UI snapshot size in remote mode: localStorage now keeps page/restoration state and drops transient multi-select, preview, and panel-open flags so repeated renders spend less time serializing disposable UI state.
- Reduce full-app DOM rebuild frequency further by splitting heavyweight views into localized rerender zones; current scheduled-render path now batches to animation frames, local state persistence skips unchanged snapshots, persistence no longer invalidates search/index caches on every write, IACUC lookup panels now stop scanning once enough suggestions are found, quantity-sheet entry status now computes counts in one pass without temporary row arrays, quantity-sheet previews now reuse cached statement/forms/page HTML, workflow detail modals now cache timeline, version, event, history, source-detail, and line-table fragments across repeated renders, large list bodies now reuse cached `tbody` HTML for quantity sheets, reimbursement ledgers, intake batches, and placement-task groups across non-structural rerenders, dashboard overview cards / facility cards / room-capacity rows now reuse cached HTML blocks instead of rebuilding every summary section on each render, and high-frequency row actions in intake / reimbursement / quantity-sheet / placement-task lists plus medium-frequency pagination / room-management / principal-rule actions now use delegated handlers instead of per-node rebinding after each render; nav view switches, dashboard quick-entry cards, slot-filter buttons, help-hint positioning, and room-access checkbox summaries now follow centralized delegated or one-time event paths.
